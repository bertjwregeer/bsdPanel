#ifndef BSDPANEL_NET_IPADDRESS6_H
#define BSDPANEL_NET_IPADDRESS6_H

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include "Math.h"

namespace bsdPanel
{
    namespace Net
    {
        /*class IpAddress6 : public Address
        {
            public:
                IpAddress6();
                IpAddress6(const IpAddress6 &);
                virtual ~IpAddress6();
                virtual bool isValid() const;
                virtual IpAddress6 operator=(const IpAddress6 &);
                virtual bool operator==(const IpAddress6 &);
            
            protected:
                virtual void output(std::ostream &) const;
                // function or invocation required to change '.' to ':' for delim
        };*/
    } // namespace Net
} // namespace bsdPanel
#endif